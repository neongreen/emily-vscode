{
  "name": "emily",
  "displayName": "Emily",
  "description": "Emily",
  "version": "1.0.0",
  "publisher": "emily",
  "repository": {
    "type": "git",
    "url": "https://github.com/neongreen/emily-vscode.git"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "files": [
    "out/",
    "README.md",
    "LICENSE"
  ],
  "contributes": {
    "commands": [
      {
        "command": "emily.cycleEmoji",
        "title": "Emily: Cycle Emoji in Header",
        "enablement": "editorLangId == markdown"
      },
      {
        "command": "emily.dumpDefinitions",
        "title": "Emily: Dump All Definitions (Developer)",
        "enablement": "true"
      }
    ],
    "keybindings": [
      {
        "command": "emily.cycleEmoji",
        "key": "ctrl+k ctrl+e",
        "mac": "cmd+k cmd+e",
        "when": "editorLangId == markdown && editorTextFocus"
      }
    ],
    "configuration": {
      "title": "Emily",
      "properties": {
        "emily.emojis": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "✅",
            "⛔",
            "🟢",
            "🟡",
            "🔴"
          ],
          "description": "List of emojis to cycle through in markdown headers"
        },
        "emily.languageFileExtensions": {
          "type": "object",
          "additionalProperties": {
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "default": {
            "haskell": [
              "*.hs"
            ],
            "mu": [
              "*.hs",
              "*.mu"
            ]
          },
          "description": "Mapping of language IDs to file extensions for definition searching"
        }
      }
    }
  },
  "scripts": {
    "pretest": "pnpm compile",
    "compile": "tsc -p ./",
    "esbuild-base": "esbuild src/extension.ts --bundle --outfile=out/extension.js --external:vscode --platform=node --target=node20",
    "esbuild-prod": "pnpm run esbuild-base -- --minify",
    "watch": "tsc -watch -p ./ --noEmit",
    "lint": "biome lint .",
    "test": "tsx test/cursor-position.test.ts"
  },
  "dependencies": {
    "execa": "9.3.1"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.1.3",
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.6.0",
    "dedent": "^1.6.0",
    "esbuild": "^0.25.8",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "packageManager": "pnpm@10.13.1"
}
